{
  "name": "opentrading",
  "version": "1.0.0",
  "description": "Secure, installable paper-trading platform for web, Android, and iOS",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run build && SERVE_DIR=dist node scripts/server.js",
    "build": "vite build",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test --experimental-test-coverage --test-coverage-exclude='test/**' --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=100 test/*.test.js",
    "test:e2e": "playwright test",
    "lint": "node --check src/app.js && node --check src/banking-ui.js && node --check src/core/trading.js && node --check src/core/news.js && node --check src/core/banking.js && node --check src/server/news-service.js && node --check src/server/bank-service.js && node --check scripts/server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/charles2ke/OpenTrading.git"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^6.1.0",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^7.6.0",
    "openid-client": "^6.8.7",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
